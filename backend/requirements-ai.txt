# Optional heavy local model runtime dependencies.
# Install only on machines intended to run PaddleOCR-VL / Qwen2.5-VL locally.

# PaddleOCR-VL provider
# Basic PaddleOCR lives in requirements-ocr.txt and can be enabled without
# installing the heavier local multimodal stack.
paddlex[ocr]==3.5.1

# Text interpretation + Qwen2.5-VL provider
# Gemma 4 support follows the Hugging Face model card guidance to use a current Transformers release.
torch>=2.5.0
transformers>=4.58.0
accelerate>=1.2.0
sentencepiece>=0.2.0
protobuf>=5.29.0
qwen-vl-utils>=0.0.10
huggingface_hub>=0.27.0
hf_xet>=1.1.0
