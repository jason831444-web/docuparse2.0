# Optional heavy local model runtime dependencies.
# Install only on machines intended to run official PaddleOCR-VL locally.

# PaddleOCR-VL 3.x/PaddleX provider experiments.
# These are not installed in the ocr-worker because they can conflict with
# the legacy PaddleOCR 2.x PP-OCRv4 runtime.
paddleocr==3.6.0
paddlex[ocr]==3.6.0
paddlepaddle>=3.0.0

huggingface_hub>=0.27.0
hf_xet>=1.1.0
